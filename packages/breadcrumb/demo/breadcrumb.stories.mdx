import { Meta, ArgsTable, Canvas, Story, Markdown } from '@storybook/addon-docs';
import { BreadcrumbContainer } from '@zendeskgarden/container-breadcrumb';
import { BreadcrumbStory } from './stories/BreadcrumbStory';
import README from '../README.md';

<Meta title="Packages/Breadcrumb" component={BreadcrumbContainer} />

# API

<ArgsTable />

# Demo

<Canvas>
  <Story
    name="Breadcrumb"
    args={{ as: 'hook', 'aria-label': 'Breadcrumbs' }}
    argTypes={{
      as: { options: ['container', 'hook'], control: 'radio', table: { category: 'Story' } },
      'aria-label': { control: 'text' }
    }}
  >
    {args => <BreadcrumbStory {...args} />}
  </Story>
</Canvas>

<Markdown>{README}</Markdown>
