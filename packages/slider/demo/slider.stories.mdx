import { Meta, ArgsTable, Canvas, Story } from '@storybook/addon-docs';
import { SliderContainer } from '@zendeskgarden/container-slider';
import { SliderStory } from './stories/SliderStory';

<Meta
  title="Packages/Slider"
  component={SliderContainer}
  args={{ as: 'hook', max: 100, min: 0, step: 1 }}
  argTypes={{
    as: { options: ['container', 'hook'], control: 'radio', table: { category: 'Story' } }
  }}
/>

# API

<ArgsTable />

# Demo

## Uncontrolled

<Canvas>
  <Story name="Uncontrolled">{args => <SliderStory {...args} />}</Story>
</Canvas>

## Controlled

<Canvas>
  <Story
    name="Controlled"
    args={{
      defaultMinValue: 3,
      defaultMaxValue: 8,
      max: 10
    }}
  >
    {args => <SliderStory {...args} />}
  </Story>
</Canvas>
