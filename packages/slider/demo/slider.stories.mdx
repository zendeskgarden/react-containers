import { Meta, ArgsTable, Canvas, Story } from '@storybook/addon-docs';
import { SliderContainer } from '@zendeskgarden/container-slider';

import { SliderStory } from './stories/SliderStory';

<Meta
  title="Packages/Slider"
  component={SliderContainer}
  args={{ as: 'hook', min: 0, max: 10, disabled: false, required: false, rtl: false, step: 1 }}
  argTypes={{
    as: {
      options: ['container', 'hook'],
      control: 'radio',
      table: { category: 'Story' }
    },
    render: {
      table: {
        disable: true
      }
    },
    environment: {
      table: {
        disable: true
      }
    },
    disabled: {
      control: 'boolean'
    },
    required: {
      control: 'boolean'
    }
  }}
/>

# API

<ArgsTable />

# Demo

## Uncontrolled

<Canvas>
  <Story
    name="Uncontrolled"
    args={{
      defaultMinValue: 3,
      defaultMaxValue: 8
    }}
  >
    {args => <SliderStory {...args} />}
  </Story>
</Canvas>
