import { Meta, ArgsTable, Canvas, Story, Markdown } from '@storybook/addon-docs';
import { KeyboardFocusContainer } from '@zendeskgarden/container-keyboardfocus';
import { KeyboardFocusStory } from './stories/KeyboardFocusStory';
import README from '../README.md';

<Meta title="Packages/KeyboardFocus" component={KeyboardFocusContainer} />

# API

<ArgsTable />

# Demo

<Canvas>
  <Story
    name="KeyboardFocus"
    args={{ as: 'hook' }}
    argTypes={{
      as: { options: ['container', 'hook'], control: 'radio', table: { category: 'Story' } }
    }}
  >
    {args => <KeyboardFocusStory {...args} />}
  </Story>
</Canvas>

<Markdown>{README}</Markdown>
