import { Meta, ArgsTable, Canvas, Story, Markdown } from '@storybook/addon-docs';
import { FieldContainer } from '@zendeskgarden/container-field';
import { FieldStory } from './stories/FieldStory';
import README from '../README.md';

<Meta title="Packages/Field" component={FieldContainer} />

# API

<ArgsTable />

# Demo

<Canvas>
  <Story
    name="Field"
    args={{ as: 'hook', hasHint: true, hasMessage: true }}
    argTypes={{
      as: { options: ['container', 'hook'], control: 'radio', table: { category: 'Story' } }
    }}
  >
    {args => <FieldStory {...args} />}
  </Story>
</Canvas>

<Markdown>{README}</Markdown>
