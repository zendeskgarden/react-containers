language: node_js

branches:
  only:
    - master

cache:
  yarn: true
  directories:
    - node-modules

before_install:
  - curl -o- -L https://yarnpkg.com/install.sh | bash
  - export PATH="$HOME/.yarn/bin:$PATH"

install: yarn --frozen-lockfile --ignore-scripts

script:
  # Bypass npm script until we add our first package so it doesn't error
  - yarn eslint utils/ .storybook/
  - yarn format

after_success:
  - yarn build-storybook

deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  local_dir: demo
  on:
    branch: master
